document.getElementById("month1").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
document.getElementById("month2").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
document.getElementById("month3").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
document.getElementById("month4").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
document.getElementById("month5").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
document.getElementById("month6").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
document.getElementById("month7").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
document.getElementById("month8").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
document.getElementById("month8").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
document.getElementById("month9").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
document.getElementById("month10").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
document.getElementById("month11").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
document.getElementById("month12").addEventListener("click", function () {
  let element = this.id;
  let index = element.split("")[element.length - 1];
  let data = getMoodFromMonth(index);
  console.log(data);
});
